# encoding: utf-8

# FIXME: Disabled until live tests are ported
# module BackupSpec
#   file = File.expand_path("../../live.yml", __FILE__)
#   LIVE = File.exist?(file) ? YAML.load_file(file) : Hash.new {|h,k| h.dup }
# end
