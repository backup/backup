sudo: required

language: ruby

services:
  - docker

before_install:
  - docker-compose build

script: "bundle exec rake ci:test"

branches:
  only:
    - master
    - develop
    - feature/docker-testing
